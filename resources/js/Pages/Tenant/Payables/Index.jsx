import Breadcrumb from '@/Components/Breadcrumb'; import Pagination from '@/Components/Pagination'; import AdminLayout from '@/Layouts/AdminLayout'; import {Head,Link} from '@inertiajs/react';
const money=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v||0);
export default function Index({payables,summary}){const cards=[['Total hutang',summary.total,'text-slate-900'],['Belum jatuh tempo',summary.upcoming,'text-indigo-700'],['Jatuh tempo hari ini',summary.today,'text-amber-700'],['Terlambat',summary.overdue,'text-rose-700']]; return <AdminLayout header="Hutang Supplier"><Head title="Hutang Supplier"/><Breadcrumb items={[{label:'Transaksi'},{label:'Hutang Supplier'}]}/><div className="mb-6"><p className="text-xs font-semibold uppercase tracking-[.2em] text-indigo-600">Kewajiban usaha</p><h2 className="mt-1 text-2xl font-bold text-slate-900">Hutang supplier</h2><p className="mt-1 text-sm text-slate-500">Pembayaran bulan ini {money(summary.paid_this_month)}</p></div><div className="mb-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,color])=><div key={label} className="rounded-2xl border border-slate-200/70 bg-white p-4 shadow-sm shadow-slate-200/40"><div className="text-xs font-medium text-slate-500">{label}</div><div className={`mt-2 text-xl font-bold ${color}`}>{money(value)}</div></div>)}</div><div className="overflow-hidden rounded-2xl border border-slate-200/70 bg-white shadow-sm shadow-slate-200/40">{payables.data.map(p=><Link key={p.id} href={route('tenant.purchases.show',p.id)} className="grid gap-2 border-b border-slate-100 p-4 hover:bg-slate-50 sm:grid-cols-[1fr_1fr_auto]"><div><b>{p.supplier.name}</b><div className="text-xs text-slate-500">{p.number}</div></div><div className="text-sm text-slate-600">Jatuh tempo {p.due_date?new Date(p.due_date).toLocaleDateString('id-ID'):'—'}</div><div className="font-bold text-rose-700">{money(p.balance_amount)}</div></Link>)}{!payables.data.length&&<div className="p-12 text-center text-sm text-slate-500">Tidak ada hutang supplier. Mantap.</div>}<div className="p-4"><Pagination links={payables.links}/></div></div></AdminLayout>}
