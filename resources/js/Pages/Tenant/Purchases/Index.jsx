import Breadcrumb from '@/Components/Breadcrumb'; import Pagination from '@/Components/Pagination'; import AdminLayout from '@/Layouts/AdminLayout'; import usePermission from '@/Hooks/usePermission'; import { Head, Link } from '@inertiajs/react';
const money = (v) => new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v||0);
const labels={paid:'Lunas',unpaid:'Belum dibayar',partial:'Sebagian',overdue:'Terlambat',void:'Dibatalkan'};
export default function Index({purchases,summary,openingCostCount}){const can=usePermission(); return <AdminLayout header="Pembelian"><Head title="Pembelian"/><Breadcrumb items={[{label:'Transaksi'},{label:'Pembelian'}]}/>
<div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-indigo-600">Persediaan masuk</p><h2 className="mt-1 text-2xl font-bold text-slate-900">Pembelian</h2><p className="mt-1 text-sm text-slate-500">Nilai bulan ini {money(summary.month_total)} · hutang {money(summary.payable)}</p></div>{can('purchases.create')&&<Link href={route('tenant.purchases.create')} className="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">+ Catat pembelian</Link>}</div>
{openingCostCount>0&&<Link href={route('tenant.purchases.opening-costs.index')} className="mb-5 flex items-center justify-between rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><span><b>{openingCostCount} bahan baku</b> perlu saldo awal HPP sebelum pembelian baru.</span><span className="font-semibold">Atur sekarang →</span></Link>}
<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="hidden grid-cols-[1.2fr_1fr_.8fr_.8fr] gap-4 border-b bg-slate-50 px-5 py-3 text-xs font-semibold uppercase tracking-wider text-slate-500 md:grid"><span>Dokumen</span><span>Supplier</span><span>Total</span><span>Status</span></div>{purchases.data.map(p=><Link key={p.id} href={route('tenant.purchases.show',p.id)} className="grid gap-2 border-b border-slate-100 px-5 py-4 hover:bg-slate-50 md:grid-cols-[1.2fr_1fr_.8fr_.8fr] md:gap-4"><div><b className="text-slate-900">{p.number}</b><div className="text-xs text-slate-500">{new Date(p.purchase_date).toLocaleDateString('id-ID')}</div></div><div className="text-sm text-slate-700">{p.supplier.name}</div><div><b className="text-sm text-slate-900">{money(p.total_amount)}</b>{Number(p.balance_amount)>0&&<div className="text-xs text-rose-600">Sisa {money(p.balance_amount)}</div>}</div><div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${p.payment_status==='paid'?'bg-emerald-50 text-emerald-700':p.payment_status==='overdue'?'bg-rose-50 text-rose-700':'bg-amber-50 text-amber-700'}`}>{labels[p.payment_status]}</span></div></Link>)}{!purchases.data.length&&<div className="p-12 text-center text-sm text-slate-500">Belum ada pembelian.</div>}<div className="p-4"><Pagination links={purchases.links}/></div></div></AdminLayout>}
